<table class="table table-striped">
    <colgroup>
        <col class="col-xs-1">
        <col class="col-xs-7">
    </colgroup>
    <thead>
    <tr>
        <th class="col-lg-3">Modue name</th>
        <th>Description</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach($this->paginator as $row) : ?>
    <tr>
        <td>
            <?php echo $row['module_name']; ?>
            <div class="link-module">
                <a href="<?php echo $this->url($row['module_route']); ?>" class="glyphicon glyphicon-new-window"></a> &nbsp;
                <a href="<?php echo $this->url($row['module_route']); ?>" class="glyphicon glyphicon-cloud-download"></a>
            </div>
        </td>
        <td><?php echo $row['module_desc']; ?></td>
    </tr>
    <?php endforeach; ?>
    </tbody>
</table>

		<?php
					// add at the end of the file after the table
					echo $this->paginationControl(
					    // the paginator object
					    $this->paginator,
					    // the scrolling style
					    'sliding',
					    array('pager.phtml', 'Application'),
					    // the route to link to when a user clicks a control link
					    array(
						             'route' => 'search'
					    )
					);
					?>
