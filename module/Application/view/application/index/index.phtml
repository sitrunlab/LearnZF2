<table class="table table-striped">
    <colgroup>
        <col class="col-xs-1">
        <col class="col-xs-7">
    </colgroup>
    <thead>
    <tr>
        <th class="col-lg-3">Module name</th>
        <th>Description</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach($this->paginator as $row) : ?>
    <tr>
        <td>
            <?php echo $row->getModuleName(); ?>
            <div class="link-module">
                <a href="<?php echo $this->url($row->getModuleRoute()); ?>" class="glyphicon glyphicon-new-window"></a> &nbsp;
                <a href="<?php echo $this->downloadbuttonlink('zip', $row->getModuleName()); ?>" class="glyphicon glyphicon-cloud-download"></a>
            </div>
        </td>
        <td><?php echo $row->getModuleDesc(); ?></td>
    </tr>
    <?php endforeach; ?>
    </tbody>
</table>

		<?php
					// add at the end of the file after the table
					echo $this->paginationControl(
					    // the paginator object
					    $this->paginator,
					    // the scrolling style
					    'sliding',
					    array('pager.phtml', 'Application'),
					    // the route to link to when a user clicks a control link
					    array(
						             'route' => 'home'
					    )
					);
					?>
